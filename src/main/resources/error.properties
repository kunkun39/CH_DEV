client.username.formatnot = email address pattern is not right
client.username.duplicate = email address already register
client.password.notsame = two times password not the same
client.code.notright = register code is right
